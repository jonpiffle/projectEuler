require 'eulerLibrary'

def check_rotations(p)
  initial = p
  while(initial != (p=p.rotate))
    if !p.to_i.prime?
      return false
    end
  end
  return true
end

primes = 100.prime_generator

count = 0
primes.each do |p|
  count+= 1 if check_rotations(p)
end

puts p
